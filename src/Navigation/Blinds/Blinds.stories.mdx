import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Blinds from '.';

export const items = [
  {
    main: 'Blog',
    subitems: [
      { sub: 'Blog1alsfkjlasfjlaskjflkasjf', path: '/' },
      { sub: 'Blog2', path: '/' },
    ],
  },
  {
    main: 'About',
    path: '/',
  },
  {
    main: 'Links',
    subitems: [
      {
        sub: 'Blog1',
        path: '/',
      },
      { sub: 'Blog2', path: '/' },
    ],
  },
];

<Meta title="Navigation/Blinds" component={Blinds} />

# Blinds

**Renders full widtj dropdown navigation**

<Preview>
  <Story name="Blinds">
    <Blinds items={items} />
  </Story>
</Preview>

## API

<ArgsTable of={Blinds} />
