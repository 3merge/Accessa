import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import {
  createMuiTheme,
  ThemeProvider,
} from '@material-ui/core/styles';
import AppBar from '@material-ui/core/AppBar';
import Box from '@material-ui/core/Box';
import Blinds from '.';
import items from '../../../cypress/fixtures/blinds.json';
import { Mast } from '../../Headers/Headers.stories.jsx';

export const darkTheme = createMuiTheme({
  palette: {
    type: 'dark',
  },
});

<Meta title="Navigation/Blinds" component={Blinds} />

# Blinds

**Renders full width dropdown navigation**

Renders dowpdown when item has submenus.

<Preview>
  <Story name="Dark">
    <ThemeProvider theme={darkTheme}>
      <AppBar position="absolute" color="transparent">
        <Blinds items={items} />
      </AppBar>
      <Box m={-2}>
        <Mast />
      </Box>
    </ThemeProvider>
  </Story>
</Preview>

<Preview>
  <Story name="Light">
    <ThemeProvider>
      <Box position="relative" zIndex="10">
        <Blinds items={items} />
      </Box>
      <Box mx={-2}>
        <Mast />
      </Box>
    </ThemeProvider>
  </Story>
</Preview>

## API

<ArgsTable of={Blinds} />
