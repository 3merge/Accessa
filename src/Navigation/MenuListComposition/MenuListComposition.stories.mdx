import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import {
  createMuiTheme,
  ThemeProvider,
} from '@material-ui/core/styles';
import AppBar from '@material-ui/core/AppBar';
import Box from '@material-ui/core/Box';
import Paper from '@material-ui/core/Paper';
import MenuListComposition from '.';
import { columns } from '../../../cypress/fixtures/footer';
import { Mast } from '../../Headers/Headers.stories.jsx';

export const darkTheme = createMuiTheme({
  palette: {
    type: 'dark',
  },
});

<Meta
  title="Navigation/MenuListComposition"
  component={MenuListComposition}
/>

<Preview>
  <Story name="Dark">
    <ThemeProvider theme={darkTheme}>
      <Paper>
        <MenuListComposition items={columns} />
        <Box>
          <Mast />
        </Box>
      </Paper>
    </ThemeProvider>
  </Story>
</Preview>

# MenuListComposition
