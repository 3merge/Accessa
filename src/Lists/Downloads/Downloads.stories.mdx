import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Downloads from './Downloads';
import {
  createMuiTheme,
  ThemeProvider,
} from '@material-ui/core/styles';
import downloadList from '../../../cypress/fixtures/fileDownloads.json';

export const darkTheme = createMuiTheme({
  palette: {
    type: 'dark',
  },
});

<Meta title="Lists/Downloads" component={Downloads} />

# Downloads

<Preview>
  <Story name="Downloads">
    <Downloads
      lists={downloadList}
      gridItem={{ xs: 12, sm: 6 }}
    />
  </Story>
</Preview>

<Preview>
  <Story name="OneColumnDownloads">
    <Downloads lists={downloadList} />
  </Story>
</Preview>

<Preview>
  <Story name="DarkDownloads">
    <ThemeProvider theme={darkTheme}>
      <Downloads
        lists={downloadList}
        gridItem={{ xs: 12, sm: 6 }}
      />
    </ThemeProvider>
  </Story>
</Preview>

## API

<ArgsTable of={Downloads} />
