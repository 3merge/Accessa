import React from 'react';
import ListItemWithNests from './ListItemWithNests';
import { shallow } from 'enzyme';

describe('ListItemWithNests', () => {
  test.todo('should toggle nested list item');
  test.todo('should not render arrow');
  test.todo('should not render nested list items');
});
