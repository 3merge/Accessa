describe('SearchOverlay', () => {
  describe('"renderSearchResult"', () => {
    it.todo(
      'should return null when paramter does not contain keys',
    );

    it.todo('should filter object of keys without values');
  });
});
