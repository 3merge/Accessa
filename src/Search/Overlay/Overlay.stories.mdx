import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Overlay from './Overlay';

export const searchRequest = (input) =>
  new Promise(
    (resolve, reject) =>
      resolve({ category: [], type: [] }),
    // reject({ error: 'error' }),
  )
    .then((d) => d)
    .catch((e) => {
      console.log('me second', e);
      return null;
    });

<Meta title="Search/Overlay" component={Overlay} />

# Overlay

<Preview>
  <Story name="Overlay">
    <Overlay searchRequest={searchRequest} />
  </Story>
</Preview>

## API

<ArgsTable of={Overlay} />
