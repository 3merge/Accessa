import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Field from './Field';
import blogList from '../../../cypress/fixtures/blogList';

<Meta title="Features/Field" component={Field} />

# Field

**Renders list of blog cards. Returns null When lists prop
is not an array or has no length**

<Preview>
  <Story name="Field">
    <Field title="Related articles" lists={blogList} />
  </Story>
</Preview>

## API

<ArgsTable of={Field} />
