import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import { Button } from '@material-ui/core';
import Lightbox from '.';
import videos from '../../../cypress/fixtures/videos';

<Meta title="Galleries/Lightbox" component={Lightbox} />

# Lightbox

**Vedio player with caption**

<Preview>
  <Story name="Lightbox">
    <Lightbox
      lists={videos.map((item) => ({
        ...item,
        description: 'This is a description',
        caption: 'And this is the video caption',
      }))}
    />
  </Story>
</Preview>

## API

<ArgsTable of={Lightbox} />
