import {
  Meta,
  Story,
  Preview,
  ArgsTable,
} from '@storybook/addon-docs/blocks';
import Lightbox from '.';

<Meta title="Galleries/Lightbox" component={Lightbox} />

# Lightbox

**Renders list of title, body and action button. Returns
null When lists prop is not an array or has no length**

<Preview>
  <Story name="Lightbox">
    <Lightbox />
  </Story>
</Preview>

## API

<ArgsTable of={Lightbox} />
