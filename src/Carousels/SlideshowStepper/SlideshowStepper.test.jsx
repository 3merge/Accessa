describe('SlideshowStepper', () => {
  it.todo(
    'should subtract number of visible slides by total slides for paginating slideshows with multiple slides per view',
  );

  it.todo(
    'should get active slide number and forward into the Stepper component',
  );

  it.todo(
    "should call onChange when the context's subscribe function is invoked",
  );
});
